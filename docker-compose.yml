version: "3.9"

services:

  vector:

    build: .

    container_name: vector-service

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app
